---
import DefaultLayout from '../../layouts/DefaultLayout.astro'

const title = 'Offcanvas'
const bodyClass = 'demo-offcanvas'
---

<DefaultLayout title={title} bodyClass={bodyClass}>
    <div class='w-divider'></div>

    <div class='demo-panel'>
        <input type='radio' name='mode' value='push' checked /> Push (default) &nbsp;
        <input type='radio' name='mode' value='overlay' /> Overlay

        <br /><br />

        <div class='offcanvas-controls'>
            <button class='w-button wz-control-left'>Offcanvas Left</button>
            <button class='w-button wz-control-right'>Offcanvas Right</button>
            <button class='w-button wz-control-top'>Offcanvas Top</button>
            <button class='w-button wz-control-bottom'>Offcanvas Bottom</button>
        </div>
    </div>
</DefaultLayout>

<style lang='scss'>
    .demo-panel {
        margin: 1.5rem 1rem;
    }
</style>

<script>
    // import {offcanvas} from 'wicle/ui/offcanvas'
    // offcanvas()

    // document.querySelectorAll<HTMLElement>('input[name=mode]').forEach(control => {
    //     control.addEventListener('change', e => {
    //         let radio = e.currentTarget as HTMLInputElement
    //         if (radio.checked) {
    //             document.querySelectorAll<HTMLElement>('[data-control*=wz-control-]').forEach(el => {
    //                 el.dataset.mode = radio.value
    //             })
    //             document.querySelectorAll<HTMLElement>('.l-site-offcanvas').forEach(el => {
    //                 console.log('---1', jQuery(el).data)
    //             })
    //         }
    //     })
    // })
</script>
